spring:
  datasource:
    url: jdbc:postgresql://db.ijtmikcskmjxubyeqrfa.supabase.co:5432/postgres
    username: postgres
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

springdoc:
  swagger-ui:
    path: /swagger-ui.html
